import { SearchTourPipe } from './search-tour.pipe';

describe('SearchTourPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTourPipe();
    expect(pipe).toBeTruthy();
  });
});
